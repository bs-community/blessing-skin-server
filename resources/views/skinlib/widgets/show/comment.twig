{% if comment_script %}
  <div class="container-fluid p-0">
    <div class="card">
      <div class="card-header">
        <h3 class="card-title">{{ trans('skinlib.show.comment') }}</h3>
      </div>
      <div class="card-body">
        {{ comment_script|raw }}
      </div>
    </div>
  </div>
{% endif %}
